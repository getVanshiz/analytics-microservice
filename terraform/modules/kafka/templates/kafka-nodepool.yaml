
apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaNodePool
metadata:
  name: ${cluster}-np-dual
  namespace: ${namespace}
  labels:
    strimzi.io/cluster: ${cluster}
spec:
  replicas: 1
  roles:
    - controller
    - broker
  storage:
    type: ephemeral
